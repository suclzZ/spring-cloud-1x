server:
  port: 8765

spring:
  application:
    name: service-feign
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
#默认断路器关闭
feign:
  hystrix:
    enabled: true



